# Dependencias gestionadas por el IDF Component Manager
# Solo necesitamos el driver de cámara (sin esp-dl para inferencia)
dependencies:
  espressif/esp32-camera:
    version: ">=2.0.0"
  espressif/mdns:
    version: ">=1.2.0"
